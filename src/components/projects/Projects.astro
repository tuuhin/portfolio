---
import { projectData } from "../../data/project_data";
import ProjectCard from "./ProjectCard.svelte";
---

<div
  class="bg-slate-300 px-5 md:px-10 lg:px-28 py-10 border-t-2 border-black h-fit max-h-min"
  id="projects"
>
  <div class="flex flex-col justify-center items-center">
    <p class="text-4xl font-bold font-inconsolata tracking-normal">
      {"Some  projects,that I had built"}
    </p>

    <p class="tracking-tight text-base font-poppins py-2 max-w-xl text-center">
      {
        "In order to grow you should should practice your skills and there is nothing more practicing than challenging yourself to make projects"
      }
    </p>
  </div>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 py-4">
    {projectData.map((e) => <ProjectCard {...e} />)}
  </div>
</div>
